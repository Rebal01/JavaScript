import chalk from 'chalk';
export function logRed(red) {
    console.log(chalk.red(`${red}`));
}
export function logGreen(green) {
    console.log(chalk.green(`${green}`));
}
export function logBlue(blue) {
    console.log(chalk.blue(`${blue}`));
}